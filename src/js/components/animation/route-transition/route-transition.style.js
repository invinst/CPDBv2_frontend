import { snowColor } from 'utils/styles';


export const overlayStyle = ({
  position: 'fixed',
  top: 0,
  left: 0,
  height: '100%',
  width: '100%',
  backgroundColor: snowColor,
  zIndex: 1000
});
