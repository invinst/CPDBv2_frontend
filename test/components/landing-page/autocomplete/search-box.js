import SearchBox from 'components/landing-page/autocomplete/search-box';

describe('SearchBox component', function () {
  it('should be renderable', function () {
    SearchBox.should.be.renderable();
  });
});
