import React from 'react';
import {
  renderIntoDocument,
  findRenderedDOMComponentWithClass,
} from 'react-addons-test-utils';

import { unmountComponentSuppressError } from 'utils/test';
import DocumentsTable from 'components/tracker/documents-page/documents-table';

describe('DocumentsTable component', function () {
  it('should render rows');

  it('should render InfiniteScroll component');

  it('should pass setDocumentShow component to Toggle component');
});
